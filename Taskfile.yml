version: '3'

tasks:
  test:
    cmds:
      - go test ./...
      - cd awsimds && go test ./...

  lint:
    cmds:
      - golangci-lint run
      - cd awsimds && golangci-lint run

  ci:
    cmds:
      - task: lint
      - task: test
